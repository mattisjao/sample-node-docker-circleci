machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t willrstern/sample-node .

test:
  override:
    - docker run willrstern/sample-node npm test

deployment:
  production:
    branch: master
    commands:
      - docker login -e jmtandberg@gmail.com -u mattisjao -p 11daips789
      - ./deploy.sh
